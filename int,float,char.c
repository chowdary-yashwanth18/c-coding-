#include<stdio.h>
int main (){
    int a = 0;
    char b = 'r';
    float c = 2.8;
    printf("The address of a is %d: the value of a is %d ",&a,a );
    printf("The address of b is %c: the value of a is %d ",&b,b );
    printf("The address of c is %d: the value of a is %d ",&c,c);
}
